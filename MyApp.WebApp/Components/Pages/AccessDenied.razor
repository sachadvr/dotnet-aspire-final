@page "/Account/AccessDenied"
@using Microsoft.AspNetCore.Components.Authorization

<PageTitle>Accès Refusé</PageTitle>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body text-center">
                    <h1 class="display-1 text-danger mb-4">🚫</h1>
                    <h2 class="card-title mb-3">Accès Refusé</h2>
                    <p class="card-text text-muted mb-4">
                        Vous n'avez pas les permissions nécessaires pour accéder à cette page.
                    </p>
                    @if (!string.IsNullOrEmpty(ReturnUrl))
                    {
                        <p class="small text-muted mb-4">
                            Page demandée : <code>@ReturnUrl</code>
                        </p>
                    }
                    <div class="d-grid gap-2">
                        <a href="/" class="btn btn-primary rounded-30">
                            <span class="bi bi-house-door"></span> Retour à l'accueil
                        </a>
                        <AuthorizeView>
                            <Authorized>
                                <a href="/my-orders" class="btn btn-outline-secondary">
                                    <span class="bi bi-cart-check"></span> Mes Commandes
                                </a>
                            </Authorized>
                            <NotAuthorized>
                                <a href="/login" class="btn btn-outline-primary">
                                    <span class="bi bi-box-arrow-in-right"></span> Se connecter
                                </a>
                            </NotAuthorized>
                        </AuthorizeView>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    [SupplyParameterFromQuery]
    public string? ReturnUrl { get; set; }
}
